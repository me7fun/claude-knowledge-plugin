# 草稿区说明

本目录（`docs/wip/`）放未定稿的文件草稿（写到一半的知识页、计划书、对照笔记）：

- 不进 git（`.gitignore` 或 `.git/info/exclude` 排除，视接线模式）、不受 wiki lint 管。
- 定稿后内容进知识库（走「Wiki 建议」提案流程），草稿即删。
- 这是项目层惯例，不是 plugin 机制——plugin 只认 `knowledgeRoot` 与 `stateDir` 两个位置。

本档可留作目录说明，也可删除。
