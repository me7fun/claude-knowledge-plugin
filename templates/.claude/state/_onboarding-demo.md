# 接线示例（读完即删）

本目录（`.claude/state/`）放跨对话的任务进度/待办：

- 不进 git（`.gitignore` 排除）、任务完结即删档。
- SessionStart 会把本目录的档案清单注入每个新对话——AI 开场即知道「做到哪、还欠什么」。
- 一个任务一档，档内写：目标、已完成、待做、下一步。

本档只是接线示例，没有要接手的进度——确认接线成功（开场注入有列出本档）后即可删除本档。
